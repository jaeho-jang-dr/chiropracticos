{
  "name": "chiropractic-kr",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "근거 기반 카이로프랙틱 — Vercel + Supabase + R2 (admin file management)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "vitest run",
    "verify": "npm run test",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.624.0",
    "@aws-sdk/s3-request-presigner": "^3.624.0",
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.0",
    "jsdom": "^25.0.0",
    "vitest": "^2.0.0"
  }
}
