# 환경변수 (anon key는 정의상 public이라 config.js는 commit OK,
# service_role key 같은 비밀은 절대 .env에 두기)
.env
.env.local
.env.*.local

# 로컬/IDE
.vscode/
.idea/
.claude/
*.swp
.DS_Store
Thumbs.db

# 빌드/캐시
node_modules/
.vercel/
.cache/

# Drive 동기화 충돌
~$*
*.tmp

# 미디어 (R2/Drive에서 별도 호스팅 — Phase 2)
# Phase 1 배포에서 미디어를 GitHub에 안 올릴 거면 아래 주석 해제:
# **/*.mp4
# **/*.m4a
# **/*.pdf
# **/*.pptx
# **/*.docx

.vercel
tmp_fn_podcasts/
